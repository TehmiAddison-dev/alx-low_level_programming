#include "main.h"
/**
* _strlen - returns the length of a string.
* @c: input a word'
* Return: Always 0.
*/
int _strlen(char *s)
{
int large = 0;
while (*(c + large) != '\0')
{
large++;
}
return (large);
}
